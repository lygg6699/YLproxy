# YLproxy 环境配置指南

> .NET SDK、Visual Studio、NuGet 包、开发工具完整配置

版本：V1.0  
最后更新：2026-07-15

---

## 📋 目录

1. [系统需求](#系统需求)
2. [.NET SDK 安装](#net-sdk-安装)
3. [Visual Studio 配置](#visual-studio-配置)
4. [项目设置](#项目设置)
5. [常见问题](#常见问题)

---

## 系统需求

### 最低要求

| 组件 | 版本 | 说明 |
|---|---|---|
| **操作系统** | Windows 10 / 11 | 仅支持 Windows |
| **.NET SDK** | 10.0.301 | 由根目录 `global.json` 固定，允许同一特性带内的最新补丁 |
| **Visual Studio** | 2022+ (可选) | 推荐，但可用 VS Code |
| **内存** | 4GB+ | 构建和运行时 |

### 检查系统要求

```powershell
# 查看 Windows 版本
Get-WmiObject win32_operatingsystem | Select-Object caption

# 查看 .NET SDK 要求
Get-Content global.json | ConvertFrom-Json | Select-Object sdk

# 验证工作区、SDK、工程入口和 3proxy 运行时
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\validate-workspace.ps1
```

### 3proxy 运行时

仓库不提交 Windows x64 运行时二进制。首次克隆后，使用固定版本和 SHA-256 校验准备本地运行时：

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\prepare-runtime.ps1
```

脚本下载 3proxy 0.9.7 官方 Release，并将 `3proxy.exe`、必需 DLL 和其他运行时文件放入 `runtime/3proxy/bin64/`。真实代理数据、运行日志和生成 cfg 文件仍只保留在本机。

---

## .NET SDK 安装

### 方式 1：从官方网站

1. 访问 https://dotnet.microsoft.com/download
2. 选择".NET 10" (或 global.json 中指定的版本)
3. 下载 Windows 安装程序
4. 运行安装程序，默认安装到 `C:\Program Files\dotnet`

### 方式 2：使用 VS Code

1. 安装 **C# Dev Kit** 扩展
2. 打开 YLproxy 文件夹
3. 提示缺失 SDK 时，点击"Install"
4. 自动下载安装（推荐）

### 方式 3：使用 Visual Studio Installer

1. 打开 **Visual Studio Installer**
2. 选择"修改"现有安装或"新建"安装
3. 勾选".NET Desktop Development"工作负载
4. 勾选".NET 10 SDK"
5. 点击"修改"安装

### 验证安装

```powershell
# 查看已安装的 .NET 版本
dotnet --list-sdks

# 输出示例：
# 10.0.100 [C:\Program Files\dotnet\sdk]

# 检查默认版本
dotnet --version
```

---

## Visual Studio 配置

### Visual Studio 2022 安装

#### 必需工作负载

- ✅ **Desktop Development with C++**（包含 .NET Desktop）
- ✅ **.NET multi-platform App UI development** (MAUI/WPF)

#### 推荐扩展

| 扩展 | 用途 |
|---|---|
| **Productivity Power Tools** | 生产力工具 |
| **ReSharper** | 代码分析 (付费，可选) |
| **GitHub Copilot** | AI 助手 |

#### 项目打开

```powershell
# 命令行打开
start YLproxy.sln

# 或手动操作
# 1. 打开 Visual Studio 2022
# 2. File → Open → Folder
# 3. 选择 e:\GZQ\YLXCX\YLproxy
# 4. VS 自动加载解决方案
```

### Visual Studio Code 配置

#### 单机工作区入口

本项目按单机工作区策略使用父目录级入口：

```powershell
code E:\GZQ\YLXCX\YLproxy.code-workspace
```

父目录工作区文件不属于 `YLproxy` 项目 Git 根目录，作为本机启动入口保留；打开后应看到唯一的 `YLproxy` 工作区文件夹。

#### 必需扩展

```powershell
# 安装 C# Dev Kit
code --install-extension ms-dotnettools.csharp

# 安装 .NET Runtime
code --install-extension ms-dotnettools.vscode-dotnet-runtime
```

#### 调试配置

父目录工作区已提供调试配置；如需在项目目录单独配置 `.vscode/launch.json`，GUI 输出路径应使用 `net10.0-windows`：

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch (GUI)",
            "type": "coreclr",
            "request": "launch",
            "program": "${workspaceFolder}/src/YLproxy.GUI/bin/Debug/net10.0-windows/YLproxy.GUI.dll",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "internalConsole"
        }
    ]
}
```

#### 验收任务

```powershell
# 默认安全检查：不启动 GUI Smoke Test
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\full-check.ps1 -SkipSmokeTest

# 完整本机检查：Smoke Test 使用临时项目副本和空代理配置
pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\full-check.ps1
```

完整 Smoke Test 不使用真实 `data/config.json`，检查结束后会关闭 GUI 进程并删除临时副本。

---

## 项目设置

### 解决方案结构

```
YLproxy/
├── src/
│   ├── YLproxy.Core/
│   ├── YLproxy.GUI/
│   ├── YLproxy.Infrastructure/
│   ├── YLproxy.Models/
│   ├── YLproxy.Proxy/
│   └── YLproxy.Utils/
├── tests/
│   └── YLproxy.Tests/
├── docs/
├── runtime/
│   └── 3proxy/
├── data/
│   ├── config.example.json
│   └── config.json（本机忽略）
├── logs/（本机忽略）
└── YLproxy.sln
```

### NuGet 包管理

#### 查看依赖

```powershell
# 列出所有 NuGet 包
dotnet list package

# 检查过期包
dotnet list package --outdated
```

#### 更新包

```powershell
# 更新特定包
dotnet package update <PackageName>

# 更新所有包
dotnet list package
```

### 构建步骤

#### 1. 清理

```powershell
# 清理所有构建产物
dotnet clean
```

#### 2. 还原

```powershell
# 还原 NuGet 包
dotnet restore
```

#### 3. 构建

```powershell
# 调试版本
dotnet build --configuration Debug

# 发布版本
dotnet build --configuration Release
```

#### 4. 运行

```powershell
# 运行 GUI 程序
dotnet run --project src/YLproxy.GUI/YLproxy.GUI.csproj

# 运行测试
dotnet test
```

---

## 常见问题

### Q1：.NET SDK 版本不匹配

**错误**：
```
error : A compatible version of the .NET SDK could not be found
```

**解决**：
```powershell
# 查看要求的版本
Get-Content global.json | ConvertFrom-Json

# 安装对应版本
# 从 https://dotnet.microsoft.com/download 下载
```

### Q2：NuGet 包无法下载

**错误**：
```
error NU1101: Unable to find package
```

**解决**：
```powershell
# 清理 NuGet 缓存
dotnet nuget locals all --clear

# 重新还原
dotnet restore --no-cache
```

### Q3：VS Code 无法识别项目

**解决**：
1. 安装 C# Dev Kit
2. 关闭并重新打开 VS Code
3. 按 F5 开始调试（会自动初始化）

### Q4：编译出现 XAML 错误

**错误**：
```
error MC3050: Cannot find the type 'MainWindow'
```

**解决**：
```powershell
# 清理并重新构建
dotnet clean
dotnet build
```

---

## 🔗 相关资源

- [.NET 官方文档](https://docs.microsoft.com/dotnet)
- [Visual Studio 下载](https://visualstudio.microsoft.com/downloads)
- [C# 学习](https://docs.microsoft.com/en-us/dotnet/csharp)

---

**最后更新**：2026-07-15
