# YLproxy 状态监控机制

> 当前代码中的 MonitorService 实现、状态流转和故障检测行为

版本：V1.0  
最后更新：2026-07-15

---

## 📋 目录

1. [监控架构](#监控架构)
2. [MonitorService 实现](#monitorservice-实现)
3. [状态流转](#状态流转)
4. [故障检测](#故障检测)
5. [后续规划](#后续规划)

---

## 监控架构

### 后台监控流程

```
┌──────────────────┐
│  应用启动 (Main) │
└────────┬─────────┘
         │
         ▼
┌────────────────────────┐
│ MonitorService.Start() │  ← 后台启动定时器
└────────┬───────────────┘
         │
         ▼
    ┌─────────┐
    │ Timer   │  5 秒间隔
    │ Tick    │  ─────────┐
    └────┬────┘           │
         │                │
         ▼                │
  ┌──────────────────┐    │
  │ CheckStatusAsync │◄───┘
  └────────┬─────────┘
           │
           ├─ 遍历所有 Running 代理
           ├─ 调用 IsRunning()
           ├─ 检查进程存活
           │
           ▼
      ┌────────────┐
      │ 进程死亡？  │
      └────┬───┬───┘
           │   └─ 是 → 标记为 Failed → 通知 UI
           └─ 否 → 保持 Running
```

### 关键参数

| 参数 | 值 | 说明 |
|---|---|---|
| **检测间隔** | 5 秒 | 每 5 秒执行一次状态检测 |
| **进程启动等待** | 最多 5 秒 | `ProxyProcessManager.Start` 等待本地端口可用 |
| **自动重试次数** | 0 | 当前实现只标记 `Failed`，不会自动重启 |

---

## MonitorService 实现

### 核心代码

```csharp
public class MonitorService : IDisposable
{
    private readonly IProxyProcessManager _processManager;
    private readonly List<ProxyItem> _proxies;
    private Timer _timer;
    private bool _isMonitoring;
    
    // 通过回调记录日志并请求 UI 刷新
    private readonly Action<string> _logAction;
    private readonly Action _refreshAction;
    
    public MonitorService(IProxyProcessManager processManager, List<ProxyItem> proxies)
    {
        _processManager = processManager;
        _proxies = proxies;
    }
    
    /// <summary>
    /// 启动监控服务
    /// </summary>
    public void Start()
    {
        if (_isMonitoring) return;
        
        _isMonitoring = true;
        
        // 创建 Timer，5 秒执行一次
        _timer = new Timer(
            state: null,
            dueTime: TimeSpan.FromSeconds(5),
            period: TimeSpan.FromSeconds(5),
            callback: async (obj) =>
            {
                try
                {
                    await CheckStatusAsync();
                }
                catch (Exception ex)
                {
                    Logger.Error($"监控服务异常: {ex.Message}");
                }
            }
        );
        
        Logger.Info("监控服务已启动");
    }
    
    /// <summary>
    /// 停止监控服务
    /// </summary>
    public void Stop()
    {
        _isMonitoring = false;
        _timer?.Dispose();
        Logger.Info("监控服务已停止");
    }
    
    /// <summary>
    /// 检查所有代理状态（5 秒执行一次）
    /// </summary>
    private async Task CheckStatusAsync()
    {
        if (!_isMonitoring) return;
        
        foreach (var proxy in _proxies)
        {
            // 只检查 Running 状态的代理
            if (proxy.Status != ProxyStatus.Running)
            {
                continue;
            }
            
            // 检查进程是否还活着
            bool isRunning = _processManager.IsRunning(proxy);
            
            if (!isRunning)
            {
                // 进程异常退出
                proxy.Status = ProxyStatus.Failed;
                
                Logger.Warning($"代理 '{proxy.Name}' 的进程异常退出");
                
                _refreshAction();
            }
        }
    }
    
    public void Dispose()
    {
        Stop();
        _timer?.Dispose();
    }
}
```

### 使用示例

```csharp
// 在 MainViewModel 中初始化
public MainViewModel()
{
    InitializeComponent();
    
    // 创建并启动监控服务
    var monitor = new MonitorService(
        () => Proxies.ToList(),
        AddLog,
        RefreshDataGrid,
        TimeSpan.FromSeconds(5));
}

// MainViewModel 通过 refreshAction 刷新 DataGrid；当前没有状态变化事件。
```

---

## 状态流转

### 当前状态模型

```
Stopped ──启动成功──> Running ──用户停止──> Stopped
   │                    │
   │                    └─进程异常退出或检测失败──> Failed
   │
   └────────启动失败──────────────────────────> Failed

Failed ──用户再次启动──> Running（成功）或 Failed（失败）
```

### 状态定义

| 状态 | 说明 | 进程 | 转移触发 |
|---|---|---|---|
| **Running** | 运行中，可转发 | 活跃 | 监控发现进程死亡或用户停止 |
| **Stopped** | 已停止 | 无 | 初始状态或用户停止 |
| **Failed** | 异常 | 无/已死亡 | 启动失败或监控发现进程退出 |

---

## 故障检测

### 检测机制

```csharp
public bool IsRunning(ProxyItem proxy)
{
    try
    {
        // 1. 检查进程是否在追踪中
        if (!_processes.TryGetValue(proxy.Id, out var process))
        {
            return false;  // 未在追踪中，判定为不运行
        }
        
        // 2. 检查进程是否已退出
        if (process.HasExited)
        {
            _processes.Remove(proxy.Id);
            return false;  // 进程已退出
        }
        
        // 3. 额外验证：检查监听端口
        if (!IsPortListening(proxy.LocalPort))
        {
            return false;  // 端口未被监听
        }
        
        return true;  // 进程正常运行
    }
    catch (Exception ex)
    {
        Logger.Error($"检查进程状态异常: {ex.Message}");
        return false;  // 异常时假设进程不运行
    }
}

private bool IsPortListening(int port)
{
    try
    {
        var ipGlobalProperties = IPGlobalProperties.GetIPGlobalProperties();
        var tcpListeners = ipGlobalProperties.GetActiveTcpListeners();
        
        return tcpListeners.Any(endpoint => endpoint.Port == port);
    }
    catch
    {
        return false;
    }
}
```

### 故障日志

```log
[2026-07-15 10:35:00] [INFO] [MonitorService] 检测代理 '美国代理01' 状态
[2026-07-15 10:35:00] [INFO] [MonitorService] 代理 '美国代理01' 进程正常 (PID: 12345)
[2026-07-15 10:40:00] [WARNING] [MonitorService] 代理 '新加坡代理01' 进程异常退出
[2026-07-15 10:40:00] [INFO] [UI] 代理状态已更新: Running → Failed
```

---

## 后续规划

```csharp
public class AutoRecoveryService
{
    public async Task RecoverFailedProxiesAsync()
    {
        foreach (var proxy in _proxies.Where(p => p.Status == ProxyStatus.Failed))
        {
            Logger.Info($"尝试自动恢复代理: {proxy.Name}");
            
            // 自动恢复尚未在当前实现中启用
            await Task.Delay(TimeSpan.FromSeconds(30));
            
            // 尝试重启
            bool success = false;
            
            if (success)
            {
                proxy.Status = ProxyStatus.Running;
                Logger.Info($"代理 {proxy.Name} 自动恢复成功");
                
                // 发送通知
                SendNotification($"代理 {proxy.Name} 已自动恢复");
            }
            else
            {
                Logger.Error($"代理 {proxy.Name} 自动恢复失败");
            }
        }
    }
}
```

---

## 📖 相关文档

- [项目主体核心.md](../项目主体核心.md)
- [04-核心功能/端口管理策略.md](端口管理策略.md)

---

**最后更新**：2026-07-15
