<UserControl x:Class="YLproxy.GUI.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:YLproxy.GUI"
             mc:Ignorable="d">
    <UserControl.Resources>
        <local:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="160"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="本机信息" Margin="0,0,0,0" Grid.Column="0">
                <StackPanel Margin="10" VerticalAlignment="Top">
                    <TextBlock Text="电脑名" Foreground="#555"/>
                    <TextBlock Text="{Binding ComputerName}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Text="IP 地址" Foreground="#555"/>
                    <TextBlock Text="{Binding IpAddress}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Text="网络状态" Foreground="#555"/>
                    <TextBlock Text="{Binding NetworkStatus}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Text="时间" Foreground="#555"/>
                    <TextBlock Text="{Binding Now, StringFormat={}{0:HH:mm:ss}}" FontWeight="SemiBold"/>
                </StackPanel>
            </GroupBox>

            <GridSplitter Grid.Column="1" Width="10" HorizontalAlignment="Stretch" />

            <GroupBox Header="操作区" Grid.Column="2" Margin="0">
                <DockPanel LastChildFill="True" Margin="10">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Left" Margin="0,0,0,10">
                        <Button Content="添加" Width="90" Margin="0,0,10,0" Command="{Binding AddCommand}"/>
                        <Button Content="删除" Width="90" Margin="0,0,10,0" Command="{Binding RemoveCommand}"/>
                        <Button Content="测试" Width="90" Margin="0,0,10,0" Command="{Binding TestCommand}" IsEnabled="{Binding IsTesting, Converter={StaticResource InverseBoolConverter}}"/>
                        <Button Content="启动" Width="90" Margin="0,0,10,0" Command="{Binding StartCommand}" IsEnabled="{Binding IsStarting, Converter={StaticResource InverseBoolConverter}}"/>
                        <Button Content="停止" Width="90" Command="{Binding StopCommand}" IsEnabled="{Binding IsStopping, Converter={StaticResource InverseBoolConverter}}"/>
                    </StackPanel>

                    <TextBlock Text="(Phase 6) 已接入添加/删除/测试/启动/停止功能。" Foreground="#666"/>
                </DockPanel>
            </GroupBox>
        </Grid>

        <GroupBox Grid.Row="1" Margin="10" Header="代理列表">
            <DataGrid ItemsSource="{Binding Proxies}" AutoGenerateColumns="False"
                      SelectionMode="Single" SelectedItem="{Binding SelectedProxy, Mode=TwoWay}"
                      HeadersVisibility="Column" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="编号" Binding="{Binding Id}" Width="80"/>
                    <DataGridTextColumn Header="上游IP" Binding="{Binding RemoteHost}" Width="*"/>
                    <DataGridTextColumn Header="上游端口" Binding="{Binding RemotePort}" Width="100"/>
                    <DataGridTextColumn Header="账号" Binding="{Binding Username}" Width="150"/>
                    <DataGridTextColumn Header="本地IP" Binding="{Binding LocalHost}" Width="150"/>
                    <DataGridTextColumn Header="本地端口" Binding="{Binding LocalPort}" Width="100"/>
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Row="2" Margin="10" Header="运行日志">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListBox Grid.Row="0" ItemsSource="{Binding Logs}" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                <Button Grid.Row="1" Content="清空日志" Width="90" Height="24" 
                        Command="{Binding ClearLogCommand}" HorizontalAlignment="Right" Margin="0,4,0,0"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>

