<Project>
  <PropertyGroup>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    <!-- CA1716: identifiers Error/Stop collide with reserved words in other CLR
         languages. This rule targets public cross-language libraries; YLproxy's
         interfaces are internal to the application, so the rule is not applicable.
         Applies to all configurations so Debug -warnaserror and Release
         TreatWarningsAsErrors judge the same rule set. All other analyzer
         warnings are fixed in code, not suppressed. -->
    <NoWarn>$(NoWarn);CA1716</NoWarn>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)' == 'Release'">
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>

    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Deterministic>true</Deterministic>
    <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>
    <SatelliteResourceLanguages>en-US;zh-CN</SatelliteResourceLanguages>
    <!-- CS1591 仅在 Release 生成 XML 文档时才会出现，故随 GenerateDocumentationFile 一并抑制。 -->
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>
</Project>
